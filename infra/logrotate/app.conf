# Example logrotate config for app logs.
#
# Copy to /etc/logrotate.d/example and replace the app name.

/var/log/example/*.log {
    daily
    rotate 14
    compress
    missingok
    notifempty
    create 0640 example example
}
